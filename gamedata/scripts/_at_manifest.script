--- _at_manifest: AlifeTactics identity. Pure data, no logic. Read by _at_init (the gate) and the validator (deps.manifest_structure).

MANIFEST = {
    name    = "AlifeTactics",
    version = "1.2.1-snapshot",
    xlibs   = "1.8.5",
}
